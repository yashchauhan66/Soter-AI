"use client";
import { useState } from "react";
export function AdminActionForm() { const [message, setMessage] = useState(""); async function submit(formData: FormData) { const raw = formData.get("value"); const action = String(formData.get("action")); const value = action === "QUOTA_BUMP" ? Number(raw) : raw || undefined; const response = await fetch("/api/admin/actions", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action, targetId: formData.get("targetId"), reason: formData.get("reason"), value }) }); const data = await response.json(); setMessage(response.ok ? `Completed. Audit ${data.auditId}` : data.message); } return <form action={submit} className="card mt-8 grid gap-3 p-5 md:grid-cols-2"><h2 className="font-semibold md:col-span-2">Audited admin action</h2><select name="action" className="input" required><option value="PLAN_OVERRIDE">Plan override</option><option value="QUOTA_BUMP">Quota bump</option><option value="REPLAY_WEBHOOK">Manual webhook replay</option><option value="RETRY_DELIVERY">Mark delivery for retry</option><option value="DISABLE_PROJECT">Disable abusive project</option><option value="ENABLE_PROJECT">Re-enable project</option></select><input name="targetId" className="input" required maxLength={200} placeholder="Organization, project, or delivery ID" /><input name="value" className="input" maxLength={200} placeholder="Plan or quota value (when needed)" /><input name="reason" className="input" required minLength={5} maxLength={500} placeholder="Required reason" /><button className="button-primary md:col-span-2">Run action</button>{message && <p className="text-sm text-cyan md:col-span-2">{message}</p>}</form>; }
