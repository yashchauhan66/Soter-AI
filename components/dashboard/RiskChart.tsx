export function RiskChart({ data }: { data: { label: string; value: number }[] }) { const max = Math.max(...data.map(x=>x.value), 1); return <div className="card p-6"><h2 className="font-semibold">Top risk types</h2><div className="mt-6 space-y-4">{data.length ? data.map(item=><div key={item.label}><div className="mb-1.5 flex justify-between text-xs"><span className="text-slate-400">{item.label.replaceAll("_"," ")}</span><span>{item.value}</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-cyan" style={{width:`${(item.value/max)*100}%`}} /></div></div>) : <p className="text-sm text-slate-500">No material risks recorded this month.</p>}</div></div>; }
