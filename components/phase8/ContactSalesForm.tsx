"use client";
import { useState } from "react";

export function ContactSalesForm() { const [message, setMessage] = useState(""); async function submit(formData: FormData) { const response = await fetch("/api/phase8/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: formData.get("name"), email: formData.get("email"), company: formData.get("company"), role: formData.get("role") || undefined, monthlyMessages: formData.get("monthlyMessages") ? Number(formData.get("monthlyMessages")) : undefined, interest: formData.get("interest"), deploymentPreference: formData.get("deploymentPreference"), message: formData.get("message") }) }); const result = await response.json(); setMessage(response.ok ? "Thanks. The sales team will review your requirements." : result.message); }
  return <form action={submit} className="grid gap-4 md:grid-cols-2"><input className="input" name="name" required placeholder="Name" /><input className="input" name="email" type="email" required placeholder="Work email" /><input className="input" name="company" required placeholder="Company" /><input className="input" name="role" placeholder="Role" /><input className="input" name="monthlyMessages" type="number" min="1" placeholder="Monthly AI messages" /><select className="input" name="interest"><option value="enterprise">Enterprise</option><option value="agency">Agency partner</option><option value="security-review">Security review</option><option value="self-hosted">Self-hosted</option><option value="other">Other</option></select><select className="input md:col-span-2" name="deploymentPreference"><option value="cloud">Cloud</option><option value="self-hosted">Self-hosted</option><option value="hybrid">Hybrid</option></select><textarea className="input min-h-32 md:col-span-2" name="message" minLength={10} required placeholder="What are you protecting and what requirements matter?" /><button className="button-primary md:col-span-2">Contact sales</button>{message && <p className="text-sm text-cyan md:col-span-2">{message}</p>}</form>;
}
