import { SectionHeading } from "@/components/ui/SectionHeading";
const faqs = [["Does this guarantee complete security?", "No. CyberRakshak Guard is a defense-in-depth risk reduction layer. It should be combined with secure model configuration, access control, testing, and human oversight."], ["Does it scan external systems?", "No. Phase 1 is defensive and only evaluates text flowing into and out of your chatbot."], ["Are Aadhaar numbers validated?", "No. The detector identifies Aadhaar-like 12-digit patterns and labels them as unverified patterns."], ["Are raw secrets stored?", "No. Secret-bearing and sensitive payloads are persisted only in redacted form."]];
export function FAQ() { return <section className="border-t border-slate-800 py-24"><div className="container-page grid gap-12 lg:grid-cols-[.7fr_1.3fr]"><SectionHeading eyebrow="FAQ" title="Straight answers about the guard" /><div className="space-y-4">{faqs.map(([q,a])=><details className="card p-5 open:border-cyan/30" key={q}><summary className="cursor-pointer font-semibold">{q}</summary><p className="mt-3 leading-7 text-slate-400">{a}</p></details>)}</div></div></section>; }
