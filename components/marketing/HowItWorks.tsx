import { SectionHeading } from "@/components/ui/SectionHeading";

const steps = [["01", "Inspect input", "Send each user message to the Input Guard before calling your model."], ["02", "Apply decision", "Block risky requests or forward the redacted safeText to your model."], ["03", "Inspect output", "Run every model response through the Output Guard before display."], ["04", "Monitor risk", "Use logs, dashboard metrics, and monthly reports to improve controls."]];
export function HowItWorks() { return <section className="border-y border-slate-800 bg-slate-950/40 py-24"><div className="container-page"><SectionHeading center eyebrow="Four steps" title="Put a guard on both sides of the model" /><div className="mt-14 grid gap-8 md:grid-cols-4">{steps.map(([n,t,c]) => <div key={n}><span className="text-4xl font-black text-cyan/35">{n}</span><h3 className="mt-4 text-lg font-semibold">{t}</h3><p className="mt-2 leading-7 text-slate-400">{c}</p></div>)}</div></div></section>; }
