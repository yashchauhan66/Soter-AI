"use client";
import { useState } from "react";
export function ForgotPasswordForm() { const [message, setMessage] = useState(""); async function submit(formData: FormData) { const response = await fetch("/api/auth/request-password-reset", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: formData.get("email") }) }); const data = await response.json(); setMessage(data.developmentResetUrl ?? data.message); } return <form action={submit} className="card mx-auto max-w-md p-8"><h1 className="text-2xl font-bold">Reset password</h1><input name="email" type="email" required className="input mt-5" placeholder="you@example.com" /><button className="button-primary mt-4 w-full">Send reset link</button>{message && <p className="mt-4 break-all text-sm text-slate-400">{message}</p>}</form>; }
export function ResetPasswordForm({ token }: { token: string }) { const [message, setMessage] = useState(""); async function submit(formData: FormData) { const response = await fetch("/api/auth/reset-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token, password: formData.get("password") }) }); const data = await response.json(); setMessage(response.ok ? "Password updated. You can sign in." : data.message); } return <form action={submit} className="card mx-auto max-w-md p-8"><h1 className="text-2xl font-bold">Choose a new password</h1><input name="password" type="password" minLength={8} maxLength={200} required className="input mt-5" /><button className="button-primary mt-4 w-full">Update password</button>{message && <p className="mt-4 text-sm text-slate-400">{message}</p>}</form>; }
