# Test/demo fixtures intentionally contain fake secrets for the detector
# suites (prompt-injection and secret-format eval data). Exclude them from
# secret scanning and push protection to avoid false-positive blocks.
paths-ignore:
  - "apps/local-ai-broker/src/__tests__/**"
  - "scripts/test/**"
  - "scripts/dev/**"
  - "scripts/ml/**"