# RAG Security Review

Controls include document scanning, OCR sandboxing, quarantine, chunk redaction, vector namespace isolation, ACL filtering, grounding guard, citation checks, and private chunk leakage detection.

Known limitation: RAG security reduces risk but cannot guarantee every poisoned document or hallucinated answer is detected.

