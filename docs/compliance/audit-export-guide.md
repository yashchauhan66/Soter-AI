# Audit export guide

Use the existing tenant-scoped audit export API and dashboard. Verify the signed manifest, restrict export access to authorized roles, deliver files through short-lived authenticated links, and record the request purpose. Phase 5 evidence also includes KMS audits, retrieval audits, RAG answer quality, classifier runs, red-team runs, security events, and SIEM delivery status.
