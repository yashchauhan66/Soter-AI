import { db } from "@/lib/db";
export const dynamic="force-dynamic";
const components=["API","Guard API","Dashboard","Webhook worker","Report worker","Billing"];
export default async function StatusPage(){const incidents=await db.incident.findMany({where:{public:true},include:{updates:{where:{public:true},orderBy:{createdAt:"desc"}}},orderBy:{startedAt:"desc"},take:20});const active=incidents.filter(i=>i.status!=="RESOLVED");return <main className="container-page py-16"><p className="eyebrow">Service status</p><h1 className="mt-2 text-4xl font-bold">{active.length?"Service advisory":"All published systems operational"}</h1><p className="mt-4 text-slate-400">Only public-safe operational information appears here. Customer logs and internal security details are never published.</p><div className="mt-10 divide-y divide-slate-800 border-y border-slate-800">{components.map(component=><div className="flex justify-between py-4" key={component}><span>{component}</span><span className={active.some(i=>i.affectedComponents.includes(component))?"text-amber-300":"text-lime"}>{active.some(i=>i.affectedComponents.includes(component))?"Advisory":"Operational"}</span></div>)}</div><section className="mt-10"><h2 className="text-xl font-semibold">Incident history</h2><div className="mt-4 space-y-4">{incidents.length?incidents.map(incident=><article className="card p-5" key={incident.id}><div className="flex justify-between gap-4"><h3 className="font-semibold">{incident.title}</h3><span className="text-sm text-cyan">{incident.status}</span></div><p className="mt-2 text-sm text-slate-400">{incident.summary}</p>{incident.updates.map(update=><p className="mt-3 border-t border-slate-800 pt-3 text-sm" key={update.id}>{update.createdAt.toLocaleString()}: {update.message}</p>)}</article>):<p className="text-slate-500">No public incidents.</p>}</div></section></main>}
