import { db } from "@/lib/db";
import { getRedis } from "@/lib/redis";
import { checkSecretStoreHealth } from "@/lib/secrets/secretStoreHealth";
import { getVectorProvider } from "@/lib/rag/vector/vectorProvider";
import { PRODUCTION_THRESHOLDS } from "@/lib/phase8/quality";
export const dynamic = "force-dynamic";
function percentile(values:number[], p:number){if(!values.length)return null;const sorted=[...values].sort((a,b)=>a-b);return sorted[Math.min(sorted.length-1,Math.ceil(sorted.length*p)-1)];}
export default async function ProductionPage(){const since=new Date(Date.now()-24*60*60*1000);const redisStart=Date.now();const [metrics,failedJobs,pendingJobs,failedWebhooks,totalWebhooks,billingFailures,detectionVolume,blockedVolume,kms,vector,dbHealth,redisHealth]=await Promise.all([db.productionMetric.findMany({where:{recordedAt:{gte:since}},take:10000}),db.backgroundJob.count({where:{status:"FAILED",createdAt:{gte:since}}}),db.backgroundJob.count({where:{status:{in:["PENDING","RUNNING"]}}}),db.webhookDelivery.count({where:{status:{in:["FAILED","DEAD_LETTER"],},createdAt:{gte:since}}}),db.webhookDelivery.count({where:{createdAt:{gte:since}}}),db.paymentEvent.count({where:{eventType:{in:["payment.failed","subscription.halted"]},receivedAt:{gte:since}}}),db.guardLog.count({where:{createdAt:{gte:since}}}),db.guardLog.count({where:{action:"BLOCK",createdAt:{gte:since}}}),checkSecretStoreHealth(),getVectorProvider().then(p=>p.healthCheck()).catch(()=>({healthy:false,provider:"unknown",message:"Unavailable",latencyMs:0,configured:false})),db.$queryRaw`SELECT 1`.then(()=>"healthy").catch(()=>"unavailable"),getRedis().get<number>("crg:production:health").then(()=>`healthy (${Date.now()-redisStart}ms)`).catch(()=>"unavailable")]);const guardLatency=metrics.filter(m=>m.metric==="guard_api_latency_ms").map(m=>m.value);const dashboardLatency=metrics.filter(m=>m.metric==="dashboard_latency_ms").map(m=>m.value);const errors=metrics.filter(m=>m.metric==="request_error").length;const requests=metrics.filter(m=>m.metric==="request_total").length;const cards=[["Guard API p50",percentile(guardLatency,.5)??"No samples","ms"],["Guard API p95",percentile(guardLatency,.95)??"No samples","ms"],["Dashboard p95",percentile(dashboardLatency,.95)??"No samples","ms"],["Error rate",requests?`${(errors/requests*100).toFixed(2)}%`:"No samples",""],["Worker backlog",pendingJobs,"jobs"],["Failed jobs",failedJobs,"24h"],["Webhook failure rate",totalWebhooks?`${(failedWebhooks/totalWebhooks*100).toFixed(2)}%`:"0%",""],["Billing failures",billingFailures,"24h"],["Detection volume",detectionVolume,"24h"],["Blocked risk volume",blockedVolume,"24h"],["Redis health",redisHealth,""],["DB health",dbHealth,""],["Vector provider",vector.healthy?`healthy (${vector.latencyMs}ms)`:vector.message,""],["KMS health",kms.healthy?"healthy":kms.message,""]];return <div><p className="eyebrow">Production operations</p><h1 className="mt-2 text-3xl font-bold">Monitoring dashboard</h1><p className="mt-3 text-slate-400">Rolling 24-hour operational view. Configure external alerting and OTLP collection before launch.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,unit])=><section className="card p-5" key={String(label)}><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-xl font-bold">{value} <span className="text-sm font-normal text-slate-500">{unit}</span></p></section>)}</div><section className="mt-8 border-t border-slate-800 pt-7"><h2 className="text-lg font-semibold">Alert thresholds</h2><div className="mt-4 grid gap-2 text-sm md:grid-cols-2">{Object.entries(PRODUCTION_THRESHOLDS).map(([key,value])=><p className="border-b border-slate-800 py-2" key={key}><span className="text-slate-400">{key}</span><span className="float-right font-semibold">{value}</span></p>)}</div></section></div>}
