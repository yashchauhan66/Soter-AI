import { IncidentForm } from "@/components/ops/AdminPhase8Forms";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function AdminSupportPage() { const [tickets, incidents] = await Promise.all([db.supportTicket.findMany({ include: { organization: { select: { name: true } }, createdBy: { select: { email: true } }, messages: { orderBy: { createdAt: "desc" }, take: 1 } }, orderBy: { updatedAt: "desc" }, take: 100 }), db.incident.findMany({ include: { updates: { orderBy: { createdAt: "desc" } } }, orderBy: { startedAt: "desc" }, take: 50 })]); return <div><p className="eyebrow">Support operations</p><h1 className="mt-2 text-3xl font-bold">Tickets and incidents</h1><div className="mt-7 grid gap-8 xl:grid-cols-[1fr_360px]"><div><h2 className="text-lg font-semibold">Ticket queue</h2><div className="mt-4 divide-y divide-slate-800 border-y border-slate-800">{tickets.map(ticket => <article className="py-4" key={ticket.id}><div className="flex justify-between gap-3"><p className="font-semibold">{ticket.ticketNumber} · {ticket.subject}</p><span className="text-sm text-cyan">{ticket.status}</span></div><p className="mt-1 text-sm text-slate-500">{ticket.organization.name} · {ticket.createdBy.email} · {ticket.category} · {ticket.priority}</p><p className="mt-2 text-sm text-slate-300">{ticket.messages[0]?.body}</p></article>)}</div><h2 className="mt-8 text-lg font-semibold">Incident timeline</h2><div className="mt-4 space-y-3">{incidents.map(incident => <section className="card p-4" key={incident.id}><div className="flex justify-between"><p className="font-semibold">{incident.title}</p><span className="text-sm">{incident.status}</span></div><p className="mt-2 text-sm text-slate-400">{incident.summary}</p></section>)}</div></div><aside><h2 className="mb-4 text-lg font-semibold">Open incident</h2><IncidentForm /></aside></div></div>; }
