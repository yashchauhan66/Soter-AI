import { FeedbackReviewForm } from "@/components/ops/AdminPhase8Forms";
import { db } from "@/lib/db";
import { detectionQualityMetrics } from "@/lib/ops/quality";
export const dynamic = "force-dynamic";
export default async function AdminDetectionQualityPage() { const rows = await db.detectionFeedback.findMany({ include: { review: true, organization: { select: { name: true } }, guardLog: { select: { action: true, riskTypes: true, redactedText: true } } }, orderBy: { createdAt: "desc" }, take: 250 }); const metrics=detectionQualityMetrics(rows.map(row=>({feedback:row.feedback,detector:row.review?.detector,accepted:row.review?.status==="ACCEPTED"}))); return <div><p className="eyebrow">Accuracy loop</p><h1 className="mt-2 text-3xl font-bold">Detection quality review</h1><div className="mt-7 grid gap-4 sm:grid-cols-4">{[["Feedback count",metrics.total],["False positive rate",`${(metrics.falsePositiveRate*100).toFixed(1)}%`],["False negative rate",`${(metrics.falseNegativeRate*100).toFixed(1)}%`],["Top noisy detector",metrics.topNoisyDetector]].map(([label,value])=><section className="card p-5" key={String(label)}><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-xl font-bold">{value}</p></section>)}</div><div className="mt-7 grid gap-4 lg:grid-cols-2">{rows.map(row=><article className="card p-5" key={row.id}><div className="flex justify-between"><p className="font-semibold">{row.feedback}</p><span className="text-sm text-cyan">{row.review?.status??"PENDING"}</span></div><p className="mt-1 text-sm text-slate-500">{row.organization.name} · {row.guardLog.action} · {row.guardLog.riskTypes.join(", ")}</p>{row.guardLog.redactedText&&<pre className="mt-3 max-h-28 overflow-auto whitespace-pre-wrap bg-slate-950 p-3 text-xs text-slate-300">{row.guardLog.redactedText}</pre>}<FeedbackReviewForm feedbackId={row.id}/></article>)}</div></div>; }
