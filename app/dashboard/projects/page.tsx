import Link from "next/link";
import { FolderKanban, Plus } from "lucide-react";
import { getCurrentUser } from "@/lib/auth";
import { db } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function ProjectsPage(){const user=await getCurrentUser();const projects=await db.project.findMany({where:{userId:user.id},select:{id:true,name:true,description:true,plan:true,_count:{select:{apiKeys:true,guardLogs:true}}},orderBy:{createdAt:"desc"},take:50});return <div><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Workspace</p><h1 className="mt-2 text-3xl font-bold">Projects</h1></div><Link href="/dashboard/projects/new" className="button-primary gap-2"><Plus size={18}/>New project</Link></div><div className="mt-7 grid gap-4 md:grid-cols-2">{projects.length?projects.map(project=><article key={project.id} className="card p-6"><div className="flex items-start justify-between"><span className="rounded-xl bg-cyan/10 p-3 text-cyan"><FolderKanban/></span><span className="rounded-full bg-slate-800 px-3 py-1 text-xs">{project.plan}</span></div><h2 className="mt-5 text-xl font-semibold">{project.name}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-slate-500">{project.description||"No description"}</p><div className="mt-5 flex gap-5 border-t border-slate-800 pt-4 text-sm text-slate-400"><span>{project._count.apiKeys} keys</span><span>{project._count.guardLogs} logs</span></div></article>):<div className="card p-10 text-center md:col-span-2"><p>No projects yet.</p></div>}</div></div>}
