import Link from "next/link";
import { getActiveOrganization } from "@/lib/auth/guards";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function SupportPage() { const active = await getActiveOrganization(); const tickets = active ? await db.supportTicket.findMany({ where: { organizationId: active.org.id }, include: { messages: { where: { internal: false }, orderBy: { createdAt: "desc" }, take: 1 } }, orderBy: { updatedAt: "desc" } }) : []; return <div><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Customer support</p><h1 className="mt-2 text-3xl font-bold">Support tickets</h1></div><Link className="button-primary" href="/dashboard/support/new">New ticket</Link></div><div className="mt-7 divide-y divide-slate-800 border-y border-slate-800">{tickets.length ? tickets.map(ticket => <article className="grid gap-2 py-5 sm:grid-cols-[1fr_auto]" key={ticket.id}><div><p className="font-semibold">{ticket.subject}</p><p className="mt-1 text-sm text-slate-500">{ticket.ticketNumber} · {ticket.category} · {ticket.messages[0]?.body ?? "No public reply yet"}</p></div><div className="text-sm"><span className="text-cyan">{ticket.status}</span><p className="mt-1 text-slate-500">{ticket.priority}</p></div></article>) : <p className="py-8 text-slate-500">No support tickets yet.</p>}</div></div>; }
