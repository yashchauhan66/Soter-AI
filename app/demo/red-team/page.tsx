import Link from "next/link";
export default function RedTeamDemoPage(){return <main className="container-page py-16"><p className="eyebrow">Authorized validation</p><h1 className="mt-2 text-4xl font-bold">Defensive red-team demo mode</h1><div className="mt-6 max-w-3xl border-l-2 border-amber-400 pl-5"><p className="text-amber-200">Runs are restricted to projects owned by the signed-in workspace. Third-party targets and offensive exploitation are not supported.</p></div><div className="mt-10 grid gap-4 md:grid-cols-3">{[["Prompt injection","Confirm policy action for instruction override patterns."],["Sensitive disclosure","Confirm secrets and PII are withheld or redacted."],["Unsafe output","Confirm risky model guidance is detected before delivery."]].map(([title,copy])=><section className="card p-5" key={title}><h2 className="font-semibold">{title}</h2><p className="mt-2 text-sm text-slate-400">{copy}</p></section>)}</div><Link className="button-primary mt-8" href="/dashboard/redteam">Open owned-project runner</Link></main>}
