export default function BenchmarksPage(){return <main className="container-page py-16"><p className="eyebrow">Benchmarks</p><h1 className="mt-2 text-4xl font-bold">Reproducible detection evaluation</h1><p className="mt-4 max-w-3xl text-slate-400">Published results should include dataset version, language mix, detector version, thresholds, sample size, and known limitations. Internal customer examples are never published.</p><div className="mt-10 grid gap-4 md:grid-cols-3">{[["Precision","Measures how often flagged examples were expected to be risky."],["Recall","Measures how many expected risky examples were detected."],["Calibration","Measures whether confidence aligns with observed outcomes."]].map(([title,copy])=><section className="card p-5" key={title}><h2 className="font-semibold">{title}</h2><p className="mt-2 text-sm text-slate-400">{copy}</p></section>)}</div><section className="mt-10 border-t border-slate-800 pt-7"><h2 className="text-xl font-semibold">Current publication status</h2><p className="mt-3 text-slate-400">No externally audited benchmark is claimed yet. Use the classifier evaluation dashboard and versioned datasets to produce reviewable results before publication.</p></section></main>}
