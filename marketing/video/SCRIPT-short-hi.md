# SoterAI video script — short cut, Hinglish

Generated by `node scripts/marketing/generate-video.mjs`. Do not hand-edit — edit `buildScenes()` in that script and re-run, so the narration, the on-screen copy and the benchmark evidence can never disagree.

- Runtime: **01:00**  ·  6 scenes  ·  30 fps
- Voice: `hi-IN-MadhurNeural` at rate `+12%` (Hindi / Hinglish (male))
- Benchmark run: 2026-07-22  ·  detection audit: readme-detection-audit-2026-09-02.txt (commit 911e8d8b)
- Required disclaimer on any frame with a number: *Self-maintained synthetic benchmark, not an independent audit.*

## Timeline

| # | In | Out | Scene | On screen | Presenter |
|---|---|---|---|---|---|
| 1 | 00:00 | 00:09 | `hook` | Your AI app checks auth. It never checks the prompt. | yes |
| 2 | 00:09 | 00:18 | `input-guard` | Blocked before the model is even called. | — |
| 3 | 00:18 | 00:28 | `output-guard` | The model sees this instead. | — |
| 4 | 00:28 | 00:37 | `agent-control` | Your agent decided to transfer ₹2,50,000. | — |
| 5 | 00:37 | 00:52 | `honesty` | 98.40% tuned. 61.54% blind. Both are ours. | yes |
| 6 | 00:52 | 01:00 | `cta` | Free playground. No signup. | yes |

## Narration

### 1. hook — 00:00 → 00:09

**On screen:** AI security guard · Your AI app checks auth. It never checks the prompt.

**Voiceover (hi):** आपका AI app auth check करता है। Prompt कभी check नहीं करता। हमला वहीं से आता है।

**Alternate track:** Your AI app checks auth. It never checks the prompt. That is where the attack arrives.

<sub>To record this line yourself: save the take as `marketing/video/voice/hook.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 8.6s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 2. input-guard — 00:09 → 00:18

**On screen:** Layer 1 — Input Guard · Blocked before the model is even called.

**Voiceover (hi):** Prompt injection — model तक पहुँचने से पहले block। p95 17.83ms, बिना network call।

**Alternate track:** Prompt injection, blocked before the model is called. 17.83ms at p95, with no network call.

<sub>To record this line yourself: save the take as `marketing/video/voice/input-guard.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 8.4s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 3. output-guard — 00:18 → 00:28

**On screen:** Layer 2 — Output Guard · The model sees this instead.

**Voiceover (hi):** Aadhaar जैसा number, PAN, live API key — response जाने से पहले redact। सारे values synthetic हैं।

**Alternate track:** Aadhaar-like numbers, PAN, live keys — redacted before the response leaves. All values synthetic.

<sub>To record this line yourself: save the take as `marketing/video/voice/output-guard.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 8.9s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 4. agent-control — 00:28 → 00:37

**On screen:** Layer 3 — Agent Control · Your agent decided to transfer ₹2,50,000.

**Voiceover (hi):** Agent ने ढाई लाख transfer करने का फ़ैसला किया। IRREVERSIBLE — human approval पर रुका, rollback window पंद्रह मिनट।

**Alternate track:** The agent decided to transfer two hundred and fifty thousand rupees. Classified irreversible, held for human approval, fifteen-minute rollback window.

<sub>To record this line yourself: save the take as `marketing/video/voice/agent-control.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 9.0s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 5. honesty — 00:37 → 00:52

**On screen:** The number most vendors cut · 98.40% tuned. 61.54% blind. Both are ours.

**Voiceover (hi):** Tuned recall 98.40%। Blind held-out 61.54%। दोनों हमारे ही numbers हैं — self-maintained synthetic benchmark, independent audit नहीं।

**Alternate track:** Tuned recall 98.40%. Blind held-out 61.54%. Both are our own numbers — a self-maintained synthetic benchmark, not an independent audit.

<sub>To record this line yourself: save the take as `marketing/video/voice/honesty.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 14.4s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 6. cta — 00:52 → 01:00

**On screen:** Try to break it · Free playground. No signup.

**Voiceover (hi):** soterai.in slash playground — signup नहीं चाहिए। तोड़ के दिखाइए, और हमें बताइए।

**Alternate track:** soterai.in slash playground — no signup. Try to break it, then tell us how.

<sub>To record this line yourself: save the take as `marketing/video/voice/cta.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 7.4s — a longer take simply holds the slide longer, nothing is cut.</sub>


## Publish copy

**Title options**

- I published the benchmark number that makes my AI firewall look bad
- Prompt injection, PII and a ₹2,50,000 agent refund — stopped live
- Your AI app checks auth. It never checks the prompt.

**First two description lines** (all that shows before *more*)

> Live walkthrough of blocking prompt injection, redacting Indian PII, and holding an AI agent mid-transfer. Includes the blind held-out benchmark result — 61.54% — that most vendors would not publish.
> Try it, no signup: https://soterai.in/playground

**Pinned comment**

```
Reproduce the numbers yourself:
  npx tsx scripts/readme-recall-audit.ts
Tuned aggregate 98.40% (1104/1122) · blind held-out 61.54% (16/26) · FPR 0.00% (0/322)
Self-maintained synthetic benchmark, not an independent audit.
Methodology: https://soterai.in/benchmark · Limitations: https://soterai.in/limitations
Found a bypass? Open an issue — that is the feedback we want.
```

**UTM for every placement**

```
?utm_source=<youtube|linkedin|x|producthunt>&utm_medium=video&utm_campaign=sep26_growth&utm_content=soterai-short-hi
```

## Pre-publish checklist

- [ ] `npm run test:marketing-video` passes — it re-checks every number in this cut against the benchmark files
- [ ] Watch it once on a phone — if the verdict pill is not legible, the asset has failed
- [ ] Frame-by-frame pass for identifiers: every one must be synthetic
- [ ] Every frame with a number also shows the self-maintained disclaimer
- [ ] The blind held-out scene is still in the cut
- [ ] 16:9, 9:16 and 1:1 exported and the captions are burned into all three
- [ ] Voice is what you will claim it is — this cut says `voiceSource: "tts"` in `manifest-short-hi.json`
- [ ] Logged in `marketing/07-metrics-tracker.md` under its asset name
