# SoterAI video script — full cut, Hinglish

Generated by `node scripts/marketing/generate-video.mjs`. Do not hand-edit — edit `buildScenes()` in that script and re-run, so the narration, the on-screen copy and the benchmark evidence can never disagree.

- Runtime: **04:20**  ·  11 scenes  ·  30 fps
- Voice: `hi-IN-MadhurNeural` at rate `+12%` (Hindi / Hinglish (male))
- Benchmark run: 2026-07-22  ·  detection audit: readme-detection-audit-2026-09-02.txt (commit 911e8d8b)
- Required disclaimer on any frame with a number: *Self-maintained synthetic benchmark, not an independent audit.*

## Timeline

| # | In | Out | Scene | On screen | Presenter |
|---|---|---|---|---|---|
| 1 | 00:00 | 00:14 | `hook` | Your AI app checks auth. It never checks the prompt. | yes |
| 2 | 00:14 | 00:32 | `surfaces` | Most tools cover one. This covers all three. | yes |
| 3 | 00:32 | 00:52 | `input-guard` | Blocked before the model is even called. | — |
| 4 | 00:52 | 01:10 | `output-guard` | The model sees this instead. | — |
| 5 | 01:10 | 01:37 | `agent-control` | Your agent decided to transfer ₹2,50,000. | — |
| 6 | 01:37 | 02:00 | `governance` | Your team already pastes company data into ChatGPT. | — |
| 7 | 02:00 | 02:37 | `layers` | 40+ services. One dashboard. | — |
| 8 | 02:37 | 02:56 | `integrate` | Two calls. Five lines. Any stack. | — |
| 9 | 02:56 | 03:35 | `honesty` | 98.40% tuned. 61.54% blind. Both are ours. | yes |
| 10 | 03:35 | 04:01 | `self-host` | Runs on your box. No telemetry leaves it. | — |
| 11 | 04:01 | 04:20 | `cta` | Free playground. No signup. | yes |

## Narration

### 1. hook — 00:00 → 00:14

**On screen:** AI security guard · Your AI app checks auth. It never checks the prompt.

**Voiceover (hi):** आपका AI app login check करता है, permission check करता है — लेकिन prompt कभी check नहीं करता। और हमला वहीं से आता है: user के prompt में, model के output में, और agent के tool call में।

**Alternate track:** Your AI app checks the login and the permissions — it never checks the prompt. And that is where the attack arrives: in the prompt, in the output, and in the agent's tool call.

<sub>To record this line yourself: save the take as `marketing/video/voice/hook.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 13.6s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 2. surfaces — 00:14 → 00:32

**On screen:** Three attack surfaces · Most tools cover one. This covers all three.

**Voiceover (hi):** Input पर prompt injection और jailbreak। Output पर PII और secret leak। Tool call पर payment, delete, deploy। एक policy engine, तीन enforcement points — और detection पूरी तरह local, CPU पर, बिना network call।

**Alternate track:** At the input, prompt injection and jailbreaks. At the output, PII and secret leakage. At the tool call, payments, deletes, deploys. One policy engine, three enforcement points — and detection runs locally on CPU, with no network call.

<sub>To record this line yourself: save the take as `marketing/video/voice/surfaces.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 17.4s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 3. input-guard — 00:32 → 00:52

**On screen:** Layer 1 — Input Guard · Blocked before the model is even called.

**Voiceover (hi):** पहली layer — Input Guard। Base64, hex, homoglyph, invisible Unicode — payload पहले decode होता है, फिर block। पंद्रह jailbreak families, और Hinglish attacks भी। p95 पर 17.83ms — extra LLM round-trip नहीं।

**Alternate track:** Layer one, the Input Guard. Base64, hex, homoglyphs, invisible Unicode — the payload is decoded first, then blocked. Fifteen jailbreak families, and Hinglish attacks too. 17.83ms at p95 — not an extra LLM round-trip.

<sub>To record this line yourself: save the take as `marketing/video/voice/input-guard.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 19.0s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 4. output-guard — 00:52 → 01:10

**On screen:** Layer 2 — Output Guard · The model sees this instead.

**Voiceover (hi):** दूसरी layer — Output Guard। Aadhaar जैसा number, PAN, UPI ID, live API key — response जाने से पहले redact। Unsafe HTML या script sink — block। India-first PII, जो लगभग कोई global vendor नहीं करता।

**Alternate track:** Layer two, the Output Guard. Aadhaar-like numbers, PAN, UPI IDs, live API keys — redacted before the response leaves. Unsafe HTML or a script sink — blocked. India-first PII, which almost no global vendor does.

<sub>To record this line yourself: save the take as `marketing/video/voice/output-guard.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 17.8s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 5. agent-control — 01:10 → 01:37

**On screen:** Layer 3 — Agent Control · Your agent decided to transfer ₹2,50,000.

**Voiceover (hi):** तीसरी layer — Agent Control। Agent ने ढाई लाख रुपये transfer करने का फ़ैसला किया। Tool call बीच में रुकता है और reversibility से classify होता है। Irreversible action human approval queue में जाता है, payload redacted। Approve के बाद execute, और पंद्रह मिनट का rollback window। हर decision action ledger में, SHA-256 hash के साथ।

**Alternate track:** Layer three, Agent Control. The agent decided to transfer two hundred and fifty thousand rupees. The tool call is intercepted and classified by reversibility. An irreversible action goes to the human approval queue with the payload redacted. Approve it and it executes, with a fifteen-minute rollback window. Every decision lands in the action ledger with a SHA-256 hash.

<sub>To record this line yourself: save the take as `marketing/video/voice/agent-control.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 26.5s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 6. governance — 01:37 → 02:00

**On screen:** Usage Governance · Your team already pastes company data into ChatGPT.

**Voiceover (hi):** अब वो problem जो हर company में है — staff company data ChatGPT, Claude, Cursor में paste कर रहा है। पाँच-step policy engine decide करता है: department, data class, provider, model। Block पर साफ़ HTTP 403 और reason header। साथ में Shadow AI discovery और compliance reports।

**Alternate track:** Now the problem every company has — staff pasting company data into ChatGPT, Claude and Cursor. A five-step policy engine decides: department, data class, provider, model. When something is blocked you get an HTTP 403 and a reason header. Plus Shadow AI discovery and compliance reports.

<sub>To record this line yourself: save the take as `marketing/video/voice/governance.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 22.8s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 7. layers — 02:00 → 02:37

**On screen:** Six defence layers · 40+ services. One dashboard.

**Voiceover (hi):** पूरा product छह layers में है। Monitor — logs और reports। Protect — agent firewall, policy engine, RAG security। Detect — shadow AI, red team lab, forensics, canary network। Control — action ledger, agent passports, escrow, memory firewall। Compliance — evidence vault, blast radius। और Manage — projects, API keys, cost firewall। चालीस से ज़्यादा services, एक dashboard। कुछ modules Preview हैं — docs में साफ़ लिखा है।

**Alternate track:** The product sits in six layers. Monitor: logs and reports. Protect: agent firewall, policy engine, RAG security. Detect: shadow AI, red team lab, forensics, canary network. Control: action ledger, agent passports, escrow, memory firewall. Compliance: evidence vault, blast radius. And Manage: projects, API keys, cost firewall. Forty plus services, one dashboard — and the Preview modules say so in their docs.

<sub>To record this line yourself: save the take as `marketing/video/voice/layers.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 36.0s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 8. integrate — 02:37 → 02:56

**On screen:** Ships where you already build · Two calls. Five lines. Any stack.

**Voiceover (hi):** Integration पाँच lines का काम है — guardInput और guardOutput, बस दो calls। Node, Python, LangChain, LlamaIndex, Vercel AI SDK, MCP gateway, n8n, और VS Code, Cursor, Windsurf। या सीधा REST API।

**Alternate track:** Integration is a five-line job — guardInput and guardOutput, just two calls. Node, Python, LangChain, LlamaIndex, the Vercel AI SDK, an MCP gateway, n8n, and VS Code, Cursor and Windsurf. Or the REST API directly.

<sub>To record this line yourself: save the take as `marketing/video/voice/integrate.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 18.4s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 9. honesty — 02:56 → 03:35

**On screen:** The number most vendors cut · 98.40% tuned. 61.54% blind. Both are ours.

**Voiceover (hi):** अब वो number जो आम तौर पर छिपाया जाता है। Aggregate recall 98.40% — लेकिन वो tuned corpora पर है, यानी regression measure। Blind held-out set पर, जहाँ कभी tuning नहीं हुई, recall 61.54% है। यही असली generalization number है — और इसीलिए ML tier मौजूद है। False positive rate 0.00%, 322 benign controls पर। यह हमारा ही synthetic benchmark है, independent audit नहीं। Command screen पर है।

**Alternate track:** Now the number that usually gets hidden. Aggregate recall is 98.40% — but that is on tuned corpora, so it is a regression measure. On the blind held-out set, never tuned against, recall is 61.54%. That is the honest generalization number, and it is why the ML tier exists. False positive rate is 0.00% on 322 benign controls. This is our own synthetic benchmark, not an independent audit. The reproduce command is on screen.

<sub>To record this line yourself: save the take as `marketing/video/voice/honesty.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 38.9s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 10. self-host — 03:35 → 04:01

**On screen:** Self-host and compliance · Runs on your box. No telemetry leaves it.

**Voiceover (hi):** पूरा stack self-host हो सकता है — docker compose up, बस इतना। कोई telemetry बाहर नहीं जाती। OWASP LLM Top 10 mapped, DPDP, GDPR, HIPAA, PCI, और SOC 2 तथा ISO 27001 के लिए evidence collection — certification का दावा नहीं। Core BUSL-1.1, 2030 में Apache-2.0।

**Alternate track:** The whole stack can be self-hosted — docker compose up, that is it. No telemetry leaves the box. OWASP LLM Top 10 mapped, DPDP, GDPR, HIPAA, PCI, and evidence collection for SOC 2 and ISO 27001 — evidence, not certification. Core is BUSL-1.1, Apache-2.0 in 2030.

<sub>To record this line yourself: save the take as `marketing/video/voice/self-host.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 25.1s — a longer take simply holds the slide longer, nothing is cut.</sub>

### 11. cta — 04:01 → 04:20

**On screen:** Try to break it · Free playground. No signup.

**Voiceover (hi):** Playground खुला है, signup नहीं चाहिए। Attack paste कीजिए, verdict देखिए — और bypass कर पाएँ तो हमें बताइए। Free शून्य रुपये, Pro दो हज़ार नौ सौ निन्यानवे प्रति महीना, Enterprise volume के हिसाब से। soterai.in — SoterAI।

**Alternate track:** The playground is open, no signup needed. Paste an attack, watch the verdict — and if you can bypass it, tell us. Free is zero rupees, Pro is two thousand nine hundred ninety-nine a month, Enterprise is volume-based. soterai.in — SoterAI.

<sub>To record this line yourself: save the take as `marketing/video/voice/cta.wav` and re-render with `--vo-dir marketing/video/voice`. Current length 18.3s — a longer take simply holds the slide longer, nothing is cut.</sub>


## Publish copy

**Title options**

- I published the benchmark number that makes my AI firewall look bad
- Prompt injection, PII and a ₹2,50,000 agent refund — stopped live
- Your AI app checks auth. It never checks the prompt.

**First two description lines** (all that shows before *more*)

> Live walkthrough of blocking prompt injection, redacting Indian PII, and holding an AI agent mid-transfer. Includes the blind held-out benchmark result — 61.54% — that most vendors would not publish.
> Try it, no signup: https://soterai.in/playground

**Pinned comment**

```
Reproduce the numbers yourself:
  npx tsx scripts/readme-recall-audit.ts
Tuned aggregate 98.40% (1104/1122) · blind held-out 61.54% (16/26) · FPR 0.00% (0/322)
Self-maintained synthetic benchmark, not an independent audit.
Methodology: https://soterai.in/benchmark · Limitations: https://soterai.in/limitations
Found a bypass? Open an issue — that is the feedback we want.
```

**UTM for every placement**

```
?utm_source=<youtube|linkedin|x|producthunt>&utm_medium=video&utm_campaign=sep26_growth&utm_content=soterai-full-hi
```

## Pre-publish checklist

- [ ] `npm run test:marketing-video` passes — it re-checks every number in this cut against the benchmark files
- [ ] Watch it once on a phone — if the verdict pill is not legible, the asset has failed
- [ ] Frame-by-frame pass for identifiers: every one must be synthetic
- [ ] Every frame with a number also shows the self-maintained disclaimer
- [ ] The blind held-out scene is still in the cut
- [ ] 16:9, 9:16 and 1:1 exported and the captions are burned into all three
- [ ] Voice is what you will claim it is — this cut says `voiceSource: "tts"` in `manifest-full-hi.json`
- [ ] Logged in `marketing/07-metrics-tracker.md` under its asset name
